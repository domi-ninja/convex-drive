{
  "name": "sync-client-2",
  "version": "1.0.0",
  "description": "-  Electron\r -  Typescript\r -  Cross-platform GUI (Windows, macOS, Linux)\r -  Supports Windows and macOS\r -  Tray icon mode (runs in background)\r -  Can run as tray icon only, but can also be opened into a window\r -  Displays the status of file sync (currently using mock data)\r -  Modern, responsive UI with real-time updates\r -  System notifications\r -  Auto-refresh sync status\r -  Progress tracking with speed and time estimates",
  "main": "dist/main.js",
  "scripts": {
    "start": "electron .",
    "dev": "concurrently \"npm run watch\" \"npm run electron-dev\"",
    "watch": "tsc -w",
    "build": "tsc",
    "electron-dev": "wait-on tcp:3000 && cross-env NODE_ENV=development electron .",
    "pack": "electron-builder --dir",
    "dist": "electron-builder",
    "postinstall": "electron-builder install-app-deps"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/react": "^19.1.6",
    "@types/react-dom": "^19.1.5",
    "electron": "^36.3.2",
    "electron-builder": "^26.0.12",
    "electron-is-dev": "^3.0.1",
    "react": "^19.1.0",
    "react-dom": "^19.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.8.3"
  },
  "devDependencies": {
    "@types/electron": "^1.4.38",
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "electron-reload": "^2.0.0-alpha.1",
    "wait-on": "^8.0.3"
  }
}
